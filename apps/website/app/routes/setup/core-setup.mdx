# Vanilla JS Setup

`refinerdb` is a the core engine that can be used
with vanilla JS/TS or any ui framework. If you are using React, you might prefer
to use `refiner-react`.

## Installation

```sh
npm install refinerdb
```

## Quick Start

import InteractiveDemo from "../../features/sandpack/InteractiveDemo";

1. Create an instance of `RefinerDB`
2. Register the Indexes
3. Insert data
4. Query data

_The following example is interactive! Play around with it!_

<InteractiveDemo slug="quick-start" />
